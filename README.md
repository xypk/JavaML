# JavaML

In **src/mlutils/java** you can find a custom library for machine learning. This library mainly includes various native Naive Bayes models, alongside a wrapper for Nd4j neural networks. **src/main.java** includes a one-liner example of using *.json* files to train and run a Gaussian Naive Bayes model on a machine failure prediction dataset.
